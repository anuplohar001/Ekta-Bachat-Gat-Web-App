# Ekta Bachat Gat - React Web env

# Backend API base URL.
# Leave unset or use "/api" when the backend is served behind the same
# origin (e.g. reverse proxy / vite dev proxy).
# Point to your backend when running the two dev servers separately:
VITE_API_BASE_URL=http://localhost:5000/api
